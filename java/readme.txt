This is the file which gives info about the particular module

1. Paramfromcommandline is the program which will take argumnts from command line and will execute the instruction in the main.

 2. Installing java from Ubuntu  sudo apt-get install openjdk-7-jre 
sudo apt-get install openjdk-7-jdk


3. switching differnet java versions

sudo update-alternatives --config java

if we want to use different java version in ubuntu 


sudo add-apt-repository ppa:webupd8team/java
sudo apt-get update
sudo apt-get install oracle-java8-installer
sudo update-alternatives --config java
